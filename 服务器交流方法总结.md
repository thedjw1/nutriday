# 服务器交流方法总结

> 这份文档只回答一件事：这套项目里，本机、手机 App、云服务器、MySQL 之间到底怎么连接，平时该用什么命令交流和排查。

---

## 一、先把关系讲清楚

这套项目不是“App 直接连数据库”。

真实链路是：

```text
Flutter App -> Node 后端 API -> MySQL
```

也就是说：

- 手机 App 只请求 HTTP API
- Node 后端负责业务处理
- MySQL 只给后端用
- App 不能也不应该直接连接 MySQL

再拆开看：

### 1. 本机和服务器怎么交流

当前最推荐的原则：

- GitHub 最新版作为唯一代码来源
- 服务器只负责 `git pull` 或覆盖完整目录
- 不要混用“旧 clone + 服务器手改 + 本地补几个文件”

本机主要通过这些方式和服务器交流：

- `scp`：上传文件
- `ssh`：登录服务器
- `curl`：测试服务器接口
- `flutter run --dart-define=...`：让 App 指向服务器

### 2. 服务器和数据库怎么交流

服务器上的 Node 后端通过环境变量连接本机 MySQL：

- `MYSQL_HOST=127.0.0.1`
- `MYSQL_PORT=3306`
- `MYSQL_USER=aihealth`
- `MYSQL_PASSWORD=你的密码`
- `MYSQL_DATABASE=ai_health`

### 3. App 和服务器怎么交流

App 通过这个基础地址访问后端：

- `https://nutriday.site/api`
- 临时调试时才考虑 `http://你的服务器IP:8080/api`
- 或 `http://你的域名/api`

当前默认开发地址在 [env.dart](D:/Android-AI-Health/lib/config/env.dart) 里，默认值还是：

```text
http://localhost:8080/api
```

真机上这个地址通常是错的，因为真机里的 `localhost` 指的是手机自己。

---

## 二、你平时最常用的 4 类交流方式

## 1. 本机上传代码到服务器

上传整个后端目录：

```bash
scp -r backend-example ubuntu@你的服务器IP:/opt/ai-health-api/
```

如果想先打包再传：

```bash
tar -czf backend-example.tar.gz backend-example
scp backend-example.tar.gz ubuntu@你的服务器IP:~/
```

登录服务器解压：

```bash
cd /opt/ai-health-api
tar -xzf ~/backend-example.tar.gz
```

后端目录的核心文件是：

- [server.js](D:/Android-AI-Health/backend-example/server.js)
- [server_complete.js](D:/Android-AI-Health/backend-example/server_complete.js)
- [cloud_state.js](D:/Android-AI-Health/backend-example/cloud_state.js)
- [init_ai_health.sql](D:/Android-AI-Health/backend-example/sql/init_ai_health.sql)

其中：

- `server.js` 是启动入口
- `server_complete.js` 是主要接口实现
- `cloud_state.js` 是云状态辅助逻辑

---

## 2. 登录服务器操作

登录：

```bash
ssh ubuntu@你的服务器IP
```

进入项目目录：

```bash
cd /opt/ai-health-api/backend-example
```

常用检查：

```bash
pwd
ls
node -v
npm -v
```

---

## 3. 服务器上测试后端接口

先测版本接口：

```bash
curl http://127.0.0.1:8080/api/version/latest
```

注册：

```bash
curl -X POST http://127.0.0.1:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"pass123456","displayName":"Test User"}'
```

登录：

```bash
curl -X POST http://127.0.0.1:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"pass123456"}'
```

登录成功后，记住返回里的 `session.token`。

再带 token 测试：

```bash
curl http://127.0.0.1:8080/api/members \
  -H "Authorization: Bearer 你的token"
```

---

## 4. 让手机 App 连接服务器

最关键的一步是启动时覆盖 API 地址。

正式环境优先这样连：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=https://nutriday.site/api
```

如果服务器直接暴露 `8080`，并且你已经确认真机允许明文 HTTP：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP:8080/api
```

如果前面用了 Nginx 反向代理，但暂时还没上 HTTPS：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP/api
```

如果指定设备：

```powershell
D:\flutter\bin\flutter.bat run -d 设备ID --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP:8080/api
```

---

## 三、服务器上最常用的运维交流命令

## 1. MySQL 相关

检查 MySQL 是否运行：

```bash
sudo systemctl status mysql
```

测试数据库账号是否可用：

```bash
mysql -u aihealth -p ai_health -e "SELECT 1;"
```

执行建表 SQL：

```bash
mysql -u aihealth -p ai_health < /opt/ai-health-api/backend-example/sql/init_ai_health.sql
```

查看有哪些表：

```bash
mysql -u aihealth -p ai_health -e "SHOW TABLES;"
```

---

## 2. Node 后端相关

安装依赖：

```bash
cd /opt/ai-health-api/backend-example
npm install
```

临时启动：

```bash
export PORT=8080
export MYSQL_HOST=127.0.0.1
export MYSQL_PORT=3306
export MYSQL_USER=aihealth
export MYSQL_PASSWORD='你的MySQL密码'
export MYSQL_DATABASE=ai_health
node server.js
```

成功标志：

```text
AI Health API running on port 8080 (mysql)
```

如果看到：

```text
AI Health API running on port 8080 (local)
```

说明后端没有连上 MySQL，而是退回到了本地 JSON。

---

## 3. systemd 服务相关

查看状态：

```bash
sudo systemctl status ai-health-api
```

启动：

```bash
sudo systemctl start ai-health-api
```

重启：

```bash
sudo systemctl restart ai-health-api
```

停止：

```bash
sudo systemctl stop ai-health-api
```

看日志：

```bash
sudo journalctl -u ai-health-api -f
```

最近 50 行日志：

```bash
sudo journalctl -u ai-health-api -n 50 --no-pager
```

---

## 4. Nginx 相关

检查配置：

```bash
sudo nginx -t
```

重启：

```bash
sudo systemctl restart nginx
```

查看状态：

```bash
sudo systemctl status nginx
```

---

## 四、怎么判断“交流是通的”

你可以按这 5 层来判断：

### 第 1 层：本机能上传到服务器

能执行：

```bash
scp -r backend-example ubuntu@你的服务器IP:/opt/ai-health-api/
```

### 第 2 层：服务器能运行后端

能执行：

```bash
cd /opt/ai-health-api/backend-example
node server.js
```

并看到：

```text
AI Health API running on port 8080 (mysql)
```

### 第 3 层：服务器本机能访问接口

```bash
curl http://127.0.0.1:8080/api/version/latest
```

能返回 JSON。

### 第 4 层：后端真的写进 MySQL

例如：

```bash
mysql -u aihealth -p ai_health -e "SELECT id,email,created_at FROM users ORDER BY created_at DESC LIMIT 5;"
```

能查到刚注册的数据。

### 第 5 层：手机 App 能访问服务器

启动 Flutter 时优先带上：

```powershell
--dart-define=AI_HEALTH_API_BASE_URL=https://nutriday.site/api
```

如果 App 不再报网络错误，并且登录、成员、设备绑定等都能正常使用，说明整条链路通了。

---

## 五、这套项目里哪些功能已经能和服务器交流

当前已经能走后端 + MySQL 的链路包括：

- 注册
- 登录
- 会话校验
- 成员管理
- 当前成员选择
- 补剂盒位绑定
- 设备绑定
- 健康数据上传
- 健康快照上传
- 计划读取与重算
- 报告分析
- 补剂分析
- 医生解释- 云成员目录
- 云状态读取与同步

启动入口在 [server.js](D:/Android-AI-Health/backend-example/server.js)，实际接口实现主要在 [server_complete.js](D:/Android-AI-Health/backend-example/server_complete.js)。

对应的 Flutter 远程客户端在 [api_health_service_client.dart](D:/Android-AI-Health/lib/data/remote/api_health_service_client.dart)。

---

## 六、最容易搞错的地方

### 1. 误以为 App 直接连数据库

不是。
必须是：

```text
App -> API -> MySQL
```

### 2. 误以为数据库建好了，App 就能用了

不是。
数据库建好只是后端准备好了一部分。
还必须让 `server.js` 真正跑起来，并且让 Flutter 指向服务器地址。

### 3. 误以为真机上的 `localhost` 是云服务器

不是。
真机上的 `localhost` 只代表手机自己。

### 4. 看到接口成功，就以为已经落库

不一定。
要再查 MySQL 表确认。

---

## 七、如果服务器 AI 卡在目录检查

出现下面这种情况时：

- 一直重复 `ls -la`
- 输出总是被截断
- 没开始执行 `npm install`、`node --check`、`curl`

处理原则：

1. 不要继续让它重复目录检查
2. 直接结束当前任务或选“取消目录检查，直接进行下一步操作”
3. 新任务里只发功能性命令：
   - `npm install`
   - `node --check server.js`
   - 写 `/etc/ai-health-api.env`
   - 导入 SQL
   - `systemctl restart ai-health-api`
   - `curl http://127.0.0.1:8080/api/version/latest`

## 八、建议你以后只按这个顺序排查

1. `mysql -u aihealth -p ai_health -e "SELECT 1;"`
2. `sudo systemctl status ai-health-api`
3. `sudo journalctl -u ai-health-api -n 50 --no-pager`
4. `curl http://127.0.0.1:8080/api/version/latest`
5. `curl` 登录拿 token
6. `curl` 测成员/设备/盒位接口
7. `mysql` 查表确认落库
8. `flutter run --dart-define=AI_HEALTH_API_BASE_URL=https://nutriday.site/api`

按这个顺序查，基本都能快速定位问题。

---

## 九、相关文档

更完整的服务器上线步骤，看这里：

- [服务器上线最终完整版本-操作清单.md](D:/Android-AI-Health/服务器上线最终完整版本-操作清单.md)
- [服务器上创建数据库-操作清单.md](D:/Android-AI-Health/服务器上创建数据库-操作清单.md)
- [服务器上部署后端-操作清单.md](D:/Android-AI-Health/服务器上部署后端-操作清单.md)


