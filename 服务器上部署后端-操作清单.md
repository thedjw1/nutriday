# 在服务器上部署 Node 后端 — 操作清单

> 适用：腾讯云 Ubuntu 服务器。
> 默认项目目录：`/opt/ai-health-api/backend-example`。
> 本文只管 Node 后端部署；MySQL 创建与建表看 [服务器上创建数据库-操作清单.md](/D:/Android-AI-Health/服务器上创建数据库-操作清单.md)。

---

## 一、先决条件

部署前先确认这几件事已经完成：

1. 代码已经上传到服务器。
2. MySQL 已安装，并且 `ai_health` 数据库、`aihealth` 用户已创建。
3. 已执行 `backend-example/sql/init_ai_health.sql` 建表。
4. 服务器上 Node 版本至少是 **18**，推荐 **20**。

先检查 Node：

```bash
node -v
npm -v
```

如果 `node -v` 小于 `18`，先安装 Node 20。

---

## 二、安装 Node.js 20（如果服务器还没有）

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

验证：

```bash
node -v
npm -v
```

---

## 三、把项目放到固定目录

如果你已经上传好了代码，只需要确认目录存在：

```bash
sudo mkdir -p /opt/ai-health-api
sudo chown -R $USER:$USER /opt/ai-health-api
```

然后把项目放进去，最后结构应类似：

```text
/opt/ai-health-api/
  backend-example/
    package.json
    server.js
    sql/
```

检查：

```bash
cd /opt/ai-health-api/backend-example
ls
```

---

## 四、安装后端依赖

```bash
cd /opt/ai-health-api/backend-example
npm install
```

当前 `package.json` 只需要这些依赖：

- `express`
- `mysql2`

---

## 五、准备环境变量文件

推荐把环境变量单独放在服务器文件里，不要写死进代码。

先创建：

```bash
sudo nano /etc/ai-health-api.env
```

填入下面内容，把密码、下载地址、OpenAI Key 换成你自己的：

```bash
PORT=8080

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=aihealth
MYSQL_PASSWORD=你的MySQL密码
MYSQL_DATABASE=ai_health

OPENAI_API_KEY=你的OpenAIKey
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

AI_HEALTH_LATEST_VERSION=1.7.0
AI_HEALTH_MIN_VERSION=1.6.0
AI_HEALTH_FORCE_UPDATE=false
AI_HEALTH_DOWNLOAD_URL=https://你的下载地址/app-release.apk
```

保存后收紧权限：

```bash
sudo chmod 600 /etc/ai-health-api.env
sudo chown root:root /etc/ai-health-api.env
```

---

## 六、先用命令行直接启动一次

先不要急着配守护进程，先确认裸跑正常。

```bash
cd /opt/ai-health-api/backend-example
set -a
source /etc/ai-health-api.env
set +a
node server.js
```

如果 MySQL 已正常连接，日志会类似：

```text
AI Health API running on port 8080 (mysql)
```

如果看到：

```text
AI Health API running on port 8080 (local)
```

说明 `MYSQL_PASSWORD` 没读到，或者 MySQL 连接失败，当前回退到了本地 JSON 模式。

按 `Ctrl + C` 停掉。

---

## 七、验证接口是否正常

重新加载环境变量：

```bash
cd /opt/ai-health-api/backend-example
set -a
source /etc/ai-health-api.env
set +a
```

### 1. 版本接口

```bash
curl http://127.0.0.1:8080/api/version/latest
```

### 2. 注册接口

```bash
curl -X POST http://127.0.0.1:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"deploy-test@example.com","password":"pass123456","displayName":"Deploy Test"}'
```

### 3. 登录接口

```bash
curl -X POST http://127.0.0.1:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"deploy-test@example.com","password":"pass123456"}'
```

返回里会有：

```json
{
  "session": {
    "token": "..."
  }
}
```

把这个 `token` 记下来。

### 4. 用 token 验证会话接口

```bash
curl http://127.0.0.1:8080/api/auth/session \
  -H "Authorization: Bearer 你的token"
```

### 5. 验证计划接口

```bash
curl -X POST http://127.0.0.1:8080/api/plan/recompute \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的token" \
  -d '{
    "memberId":"member-demo",
    "fusedHealthProfile":{"weightKg":68.5},
    "nutritionGaps":{"deficits":{"protein":30,"vitaminC":200,"iron":5}}
  }'
```

---

## 八、推荐方案：用 systemd 守护

如果你只选一种守护方式，优先选 **systemd**。

创建服务文件：

```bash
sudo nano /etc/systemd/system/ai-health-api.service
```

写入：

```ini
[Unit]
Description=AI Health API
After=network.target mysql.service
Wants=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/ai-health-api/backend-example
EnvironmentFile=/etc/ai-health-api.env
ExecStart=/usr/bin/node /opt/ai-health-api/backend-example/server.js
Restart=always
RestartSec=5
KillSignal=SIGINT
TimeoutStopSec=20

[Install]
WantedBy=multi-user.target
```

注意：

- `User=ubuntu` 要改成你服务器上实际运行项目的用户。
- `ExecStart` 用 `which node` 查到的真实路径替换更稳。

先查 Node 路径：

```bash
which node
```

如果不是 `/usr/bin/node`，把服务文件里的路径改掉。

加载并启动：

```bash
sudo systemctl daemon-reload
sudo systemctl enable ai-health-api
sudo systemctl start ai-health-api
```

查看状态：

```bash
sudo systemctl status ai-health-api
```

看日志：

```bash
sudo journalctl -u ai-health-api -f
```

常用命令：

```bash
sudo systemctl restart ai-health-api
sudo systemctl stop ai-health-api
sudo systemctl status ai-health-api
```

---

## 九、可选方案：用 PM2 守护

如果你更习惯 Node 生态，也可以用 `pm2`。
但 **systemd 和 pm2 二选一**，不要两个都开。

安装：

```bash
sudo npm install -g pm2
```

启动：

```bash
cd /opt/ai-health-api/backend-example
set -a
source /etc/ai-health-api.env
set +a
pm2 start server.js --name ai-health-api
```

查看：

```bash
pm2 status
pm2 logs ai-health-api
```

开机自启：

```bash
pm2 startup
```

它会输出一条 `sudo ...` 命令，你把那条再执行一次。

然后保存当前进程：

```bash
pm2 save
```

常用命令：

```bash
pm2 restart ai-health-api
pm2 stop ai-health-api
pm2 delete ai-health-api
pm2 logs ai-health-api
```

---

## 十、开放防火墙端口（如果要外网访问）

如果你要让手机或前端通过公网访问后端，需要放行端口。

如果系统启用了 `ufw`：

```bash
sudo ufw allow 8080/tcp
sudo ufw status
```

如果你用腾讯云安全组，也要在控制台里额外放行：

- 入站 TCP `8080`

如果后面你改用 `nginx + 80/443`，那就放行 `80` 和 `443` 即可。

---

## 十一、推荐再加一层 Nginx 反向代理

如果你要长期跑，建议不要直接把 Node 暴露在公网。

安装 Nginx：

```bash
sudo apt install -y nginx
```

创建站点配置：

```bash
sudo nano /etc/nginx/sites-available/ai-health-api
```

写入：

```nginx
server {
    listen 80;
    server_name 你的域名或服务器IP;

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用：

```bash
sudo ln -s /etc/nginx/sites-available/ai-health-api /etc/nginx/sites-enabled/ai-health-api
sudo nginx -t
sudo systemctl restart nginx
```

这样前端的 API 地址就可以写成：

```text
http://你的服务器IP/api
```

或者：

```text
https://你的域名/api
```

---

## 十二、Flutter 真机连接服务器的启动命令

如果手机要连你的服务器，不要再用默认的 `localhost`。
启动 Flutter 时这样传：

```powershell
D:\flutter\bin\flutter.bat run -d 3223f54b --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP:8080/api
```

如果你已经用了 Nginx：

```powershell
D:\flutter\bin\flutter.bat run -d 3223f54b --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP/api
```

---

## 十三、升级发布时怎么做

每次你改了后端代码，服务器上按这个顺序更新：

```bash
cd /opt/ai-health-api/backend-example
npm install
```

如果你用 `systemd`：

```bash
sudo systemctl restart ai-health-api
sudo systemctl status ai-health-api
```

如果你用 `pm2`：

```bash
pm2 restart ai-health-api
pm2 status
```

---

## 十四、常见问题

### 1. 启动后显示 `(local)` 不是 `(mysql)`

说明后端没成功连接 MySQL。优先检查：

```bash
cat /etc/ai-health-api.env
```

确认这些值存在且正确：

- `MYSQL_HOST`
- `MYSQL_PORT`
- `MYSQL_USER`
- `MYSQL_PASSWORD`
- `MYSQL_DATABASE`

然后手动验证 MySQL：

```bash
mysql -u aihealth -p ai_health -e "SELECT 1;"
```

### 2. `fetch is not defined`

说明 Node 太老。升级到 Node 18+，推荐 Node 20。

### 3. `EADDRINUSE: address already in use :::8080`

说明 8080 已被别的进程占了。

查占用：

```bash
sudo lsof -i :8080
```

### 4. 手机连不上服务器

先检查：

1. Node 服务已启动
2. 云服务器安全组已放行
3. `ufw` 已放行
4. Flutter 用的是服务器 IP，不是 `localhost`

---

## 十五、小结

如果你要最稳的正式部署，按这个顺序：

1. 安装 Node 20
2. 上传项目到 `/opt/ai-health-api/backend-example`
3. `npm install`
4. 创建 `/etc/ai-health-api.env`
5. 命令行 `node server.js` 先裸跑验证
6. 用 `systemd` 托管
7. 外网访问时再配 `nginx`

如果你只想尽快跑起来，最短路径是：

```bash
cd /opt/ai-health-api/backend-example
set -a
source /etc/ai-health-api.env
set +a
node server.js
```

确认接口没问题后，再补 `systemd`。
