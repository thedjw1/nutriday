# AI Health 服务器上线最终完整版本 — 操作清单

> 目标：把当前项目的 `backend-example` 完整上传到腾讯云 Ubuntu 服务器，接入 MySQL，并让手机 App 正常连接服务器使用。
>
> 结论先说：
> - App **不是直接连接 MySQL**
> - App 是先请求 **Node 后端 API**
> - Node 后端再把数据写入 **MySQL**
>
> 所以要想“App 正常使用”，必须同时满足：
> - 服务器上的 Node 后端已经启动，并且日志显示 `AI Health API running on port 8080 (mysql)`
> - 你的 Flutter App 已经改成连接服务器地址，而不是 `localhost`

---

## 一、你现在真正要做的事情

只做这 6 件事：

1. 把整个 `backend-example` 上传到服务器
2. 在服务器安装并配置 MySQL
3. 执行 `backend-example/sql/init_ai_health.sql`
4. 在服务器安装 Node.js 并运行 `server.js`
5. 用 `systemd` 做成常驻服务
6. 启动 Flutter App 时，把 API 地址改成你的服务器

补一句关键事实：

- `server.js` 只是启动入口
- 实际接口实现主要在 [server_complete.js](D:/Android-AI-Health/backend-example/server_complete.js)
- 云状态辅助逻辑在 [cloud_state.js](D:/Android-AI-Health/backend-example/cloud_state.js)

所以如果服务器上的 `backend-example` 缺了 `server_complete.js` 或 `cloud_state.js`，`node server.js` 不能算完整部署。

---

## 二、先确认当前项目已经支持什么

当前仓库里，这几部分已经接好了：

- 后端启动入口：[server.js](D:/Android-AI-Health/backend-example/server.js)
- 后端主运行时：[server_complete.js](D:/Android-AI-Health/backend-example/server_complete.js)
- 云状态辅助逻辑：[cloud_state.js](D:/Android-AI-Health/backend-example/cloud_state.js)
- 建表 SQL：[init_ai_health.sql](D:/Android-AI-Health/backend-example/sql/init_ai_health.sql)
- Flutter 远程接口客户端：[api_health_service_client.dart](D:/Android-AI-Health/lib/data/remote/api_health_service_client.dart)
- Flutter 云端同步服务：[cloud_health_service.dart](D:/Android-AI-Health/lib/data/remote/cloud_health_service.dart)
- 成员同步仓库：[syncing_member_repository.dart](D:/Android-AI-Health/lib/data/runtime/syncing_member_repository.dart)
- 运行时接线：[app_runtime_facade.dart](D:/Android-AI-Health/lib/data/runtime/app_runtime_facade.dart)
- App 启动注入：[main.dart](D:/Android-AI-Health/lib/main.dart)
- App 默认 API 地址配置：[env.dart](D:/Android-AI-Health/lib/config/env.dart)

当前后端已经支持这些接口：

- `GET /api/version`
- `GET /api/version/latest`
- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/auth/session`
- `POST /api/auth/logout`
- `GET /api/members`
- `POST /api/members`
- `PUT /api/members/:id`
- `DELETE /api/members/:id`
- `POST /api/members/:id/select`
- `GET /api/cloud/members`
- `PUT /api/cloud/members/:id`
- `GET /api/cloud/state`
- `PATCH /api/cloud/state`
- `GET /api/supplement-box-bindings`
- `POST /api/supplement-box-bindings`
- `GET /api/device-bindings`
- `POST /api/device-bindings`
- `POST /api/health-data`
- `POST /api/health/snapshots`
- `POST /api/report/analyze`
- `POST /api/supplement/analyze`
- `POST /api/plan/recompute`
- `GET /api/plan/latest`
- `POST /api/doctor/explain`

当前 MySQL 建表脚本已经包含这些核心表：

- `users`
- `auth_sessions`
- `members`
- `device_bindings`
- `supplement_box_bindings`
- `health_data_log`
- `health_snapshots`
- `generated_plans`
- `analysis_records`
- `sync_events`

---

## 三、推荐的服务器目录结构

下面用这个目录举例：

```text
/opt/ai-health-api/backend-example
```

如果你的目录不是这个，后面命令里的路径按实际改掉。

---

## 四、本机先上传整个后端目录

不要只上传一个 SQL 文件。
这次应该上传整个 `backend-example`。

### 方式 A：直接上传整个目录

在本机项目根目录执行：

```bash
scp -r backend-example ubuntu@你的服务器IP:/opt/ai-health-api/
```

如果 `/opt/ai-health-api` 还不存在，可以先登录服务器创建：

```bash
sudo mkdir -p /opt/ai-health-api
sudo chown -R ubuntu:ubuntu /opt/ai-health-api
```

### 方式 B：先打包，再上传

本机执行：

```bash
tar -czf backend-example.tar.gz backend-example
scp backend-example.tar.gz ubuntu@你的服务器IP:~/
```

服务器执行：

```bash
sudo mkdir -p /opt/ai-health-api
sudo chown -R ubuntu:ubuntu /opt/ai-health-api
cd /opt/ai-health-api
tar -xzf ~/backend-example.tar.gz
```

上传后，服务器上应该能看到：

```bash
ls /opt/ai-health-api/backend-example
```

里面至少应有：

- `server.js`
- `server_complete.js`
- `cloud_state.js`
- `package.json`
- `sql/init_ai_health.sql`

如果你是通过 GitHub 中转部署，原则也一样：

- 以 GitHub 最新版为唯一代码来源
- 不要再混用“旧 clone + 服务器手改 + 本地临时上传”
- 每次部署都以完整的 `backend-example` 为准

---

## 五、在服务器安装 MySQL

```bash
sudo apt update
sudo apt install -y mysql-server
sudo systemctl start mysql
sudo systemctl enable mysql
sudo systemctl status mysql
```

确认输出里有：

```text
Active: active (running)
```

---

## 六、做 MySQL 安全初始化

```bash
sudo mysql_secure_installation
```

建议这样选：

- `Validate Password Plugin`：`Y`
- 密码强度：`1` 或 `2`
- root 密码：设一个强密码并保存
- `Remove anonymous users?`：`Y`
- `Disallow root login remotely?`：`Y`
- `Remove test database?`：`Y`
- `Reload privilege tables?`：`Y`

---

## 七、创建数据库和业务账号

把下面的 `你的强密码` 改成你自己的。

```bash
sudo mysql -e "
CREATE DATABASE IF NOT EXISTS ai_health CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'aihealth'@'localhost' IDENTIFIED BY '你的强密码';
GRANT ALL PRIVILEGES ON ai_health.* TO 'aihealth'@'localhost';
FLUSH PRIVILEGES;
"
```

验证：

```bash
mysql -u aihealth -p ai_health -e "SELECT 'OK' AS status;"
```

如果输出 `OK`，说明数据库和账号可用。

---

## 八、执行项目建表 SQL

因为你已经上传了整个 `backend-example`，所以直接在服务器执行：

```bash
mysql -u aihealth -p ai_health < /opt/ai-health-api/backend-example/sql/init_ai_health.sql
```

执行完后，可以确认表已经建立：

```bash
mysql -u aihealth -p ai_health -e "SHOW TABLES;"
```

应至少能看到：

- `users`
- `members`
- `auth_sessions`
- `device_bindings`
- `supplement_box_bindings`
- `health_data_log`
- `health_snapshots`
- `generated_plans`
- `analysis_records`
- `sync_events`

---

## 九、在服务器安装 Node.js

推荐 Node 20。

### 方式 A：系统仓库已有可用版本时

```bash
sudo apt install -y nodejs npm
node -v
npm -v
```

### 方式 B：如果版本太旧，使用 NodeSource

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
node -v
npm -v
```

如果 `node -v` 显示的是 `v20.x`，就可以继续。

---

## 十、安装后端依赖

进入后端目录：

```bash
cd /opt/ai-health-api/backend-example
npm install
```

如果装完没有报错，说明后端依赖已经准备好。

---

## 十一、先用环境变量裸跑一次后端

先不要急着上 `systemd`。
先用最直接的方式把服务跑起来，确认数据库真的接通。

```bash
export PORT=8080
export MYSQL_HOST=127.0.0.1
export MYSQL_PORT=3306
export MYSQL_USER=aihealth
export MYSQL_PASSWORD='你的MySQL密码'
export MYSQL_DATABASE=ai_health
cd /opt/ai-health-api/backend-example
node server.js
```

如果成功，日志应该类似：

```text
AI Health API running on port 8080 (mysql)
```

如果显示的是：

```text
AI Health API running on port 8080 (local)
```

说明它没有连上 MySQL，而是回退到了本地 JSON 文件模式。
这时先不要继续，先检查数据库密码和建表是否正确。

---

## 十二、先在服务器本机验证接口

保持 `node server.js` 运行，另开一个终端窗口测试。

### 1. 版本接口

```bash
curl http://127.0.0.1:8080/api/version/latest
```

### 2. 注册接口

```bash
curl -X POST http://127.0.0.1:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"finalcheck@example.com","password":"pass123456","displayName":"Final Check"}'
```

### 3. 登录接口

```bash
curl -X POST http://127.0.0.1:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"finalcheck@example.com","password":"pass123456"}'
```

登录成功后，返回里会有：

- `user`
- `session.token`

把这个 `session.token` 记下来，下面接口都要带它。

---

## 十三、验证这次补上的 3 条关键链路

这一步是这轮改动最关键的验证。

### 1. 成员接口

先查当前成员列表：

```bash
curl http://127.0.0.1:8080/api/members \
  -H "Authorization: Bearer 你的token"
```

创建一个成员：

```bash
curl -X POST http://127.0.0.1:8080/api/members \
  -H "Authorization: Bearer 你的token" \
  -H "Content-Type: application/json" \
  -d '{"displayName":"爸爸","gender":"male","relationship":"father"}'
```

选择当前成员：

```bash
curl -X POST http://127.0.0.1:8080/api/members/成员ID/select \
  -H "Authorization: Bearer 你的token"
```

### 2. 补剂盒位绑定接口

```bash
curl -X POST http://127.0.0.1:8080/api/supplement-box-bindings \
  -H "Authorization: Bearer 你的token" \
  -H "Content-Type: application/json" \
  -d '{"memberId":"成员ID","slotId":"A1","supplementName":"Vitamin D3","dosage":"1 capsule daily"}'
```

然后查询：

```bash
curl http://127.0.0.1:8080/api/supplement-box-bindings \
  -H "Authorization: Bearer 你的token"
```

### 3. 设备绑定接口

```bash
curl -X POST http://127.0.0.1:8080/api/device-bindings \
  -H "Authorization: Bearer 你的token" \
  -H "Content-Type: application/json" \
  -d '{"memberId":"成员ID","deviceId":"band-001","deviceName":"Health Band","deviceType":"wearable","isPrimary":true}'
```

然后查询：

```bash
curl http://127.0.0.1:8080/api/device-bindings \
  -H "Authorization: Bearer 你的token"
```

---

## 十四、直接查 MySQL，确认不是假成功

接口返回成功还不够，必须直接查表。

### 1. 查用户表

```bash
mysql -u aihealth -p ai_health -e "SELECT id,email,display_name,current_member_id,created_at FROM users ORDER BY created_at DESC LIMIT 10;"
```

### 2. 查登录会话

```bash
mysql -u aihealth -p ai_health -e "SELECT token,email,display_name,created_at FROM auth_sessions ORDER BY created_at DESC LIMIT 10;"
```

### 3. 查成员

```bash
mysql -u aihealth -p ai_health -e "SELECT id,user_id,display_name,gender,relationship,is_current,created_at FROM members ORDER BY created_at DESC LIMIT 10;"
```

### 4. 查补剂盒位绑定

```bash
mysql -u aihealth -p ai_health -e "SELECT id,user_id,member_id,slot_id,supplement_name,created_at FROM supplement_box_bindings ORDER BY created_at DESC LIMIT 10;"
```

### 5. 查设备绑定

```bash
mysql -u aihealth -p ai_health -e "SELECT id,user_id,member_id,device_id,device_name,device_type,is_primary,created_at FROM device_bindings ORDER BY created_at DESC LIMIT 10;"
```

如果这些表里都有你刚刚插入的数据，说明这轮云端化链路是通的。

---

## 十五、确认原有云端接口也正常

继续确认原来已经支持的功能没有被破坏。

### 1. 健康数据

```bash
curl -X POST http://127.0.0.1:8080/api/health-data \
  -H "Authorization: Bearer 你的token" \
  -H "Content-Type: application/json" \
  -d '{"source":"manual-check","heartRate":72,"steps":5200}'
```

### 2. 健康快照

```bash
curl -X POST http://127.0.0.1:8080/api/health/snapshots \
  -H "Authorization: Bearer 你的token" \
  -H "Content-Type: application/json" \
  -d '{"source":"manual-snapshot","summary":{"weightKg":65.2,"bodyFatPct":21.4}}'
```

### 3. 最新计划

```bash
curl http://127.0.0.1:8080/api/plan/latest \
  -H "Authorization: Bearer 你的token"
```

如需继续确认，可再查：

```bash
mysql -u aihealth -p ai_health -e "SELECT id,user_id,created_at FROM health_data_log ORDER BY id DESC LIMIT 10;"
mysql -u aihealth -p ai_health -e "SELECT id,user_id,created_at FROM health_snapshots ORDER BY id DESC LIMIT 10;"
mysql -u aihealth -p ai_health -e "SELECT id,user_id,version,created_at FROM generated_plans ORDER BY id DESC LIMIT 10;"
```

---

## 十六、确认裸跑没问题后，改成 systemd 常驻服务

先创建环境变量文件：

```bash
sudo nano /etc/ai-health-api.env
```

写入：

```bash
PORT=8080
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=aihealth
MYSQL_PASSWORD=你的MySQL密码
MYSQL_DATABASE=ai_health
```

保存后，创建服务文件：

```bash
sudo nano /etc/systemd/system/ai-health-api.service
```

写入：

```ini
[Unit]
Description=AI Health API
After=network.target mysql.service
Wants=mysql.service

[Service]
Type=simple
WorkingDirectory=/opt/ai-health-api/backend-example
EnvironmentFile=/etc/ai-health-api.env
ExecStart=/usr/bin/node /opt/ai-health-api/backend-example/server.js
Restart=always
RestartSec=5
User=ubuntu
Group=ubuntu

[Install]
WantedBy=multi-user.target
```

如果 `node` 不在 `/usr/bin/node`，先执行：

```bash
which node
```

然后把 `ExecStart` 改成实际路径。

启用服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable ai-health-api
sudo systemctl start ai-health-api
sudo systemctl status ai-health-api
```

看日志：

```bash
sudo journalctl -u ai-health-api -f
```

正确状态应显示后端是：

```text
AI Health API running on port 8080 (mysql)
```

---

## 十七、可选：用 Nginx 反向代理

如果你不想让手机直接访问 `:8080`，可以再加一层 Nginx。

安装：

```bash
sudo apt install -y nginx
```

创建配置：

```bash
sudo nano /etc/nginx/sites-available/ai-health-api
```

写入：

```nginx
server {
    listen 80;
    server_name 你的域名或服务器IP;

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用：

```bash
sudo ln -sf /etc/nginx/sites-available/ai-health-api /etc/nginx/sites-enabled/ai-health-api
sudo nginx -t
sudo systemctl restart nginx
```

如果用了 Nginx，App 里建议优先走：

- `https://nutriday.site/api`
- 临时调试时才用 `http://你的服务器IP/api`

---

## 十八、让 Flutter App 连服务器

这一步没做，App 还是不能正常用。

当前 App 默认地址在 [env.dart](D:/Android-AI-Health/lib/config/env.dart)，开发默认值是：

```text
http://localhost:8080/api
```

这在真机上是不对的。
真机里的 `localhost` 指的是手机自己，不是你的云服务器。

所以你要这样启动 Flutter：

默认推荐：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=https://nutriday.site/api
```

原因：

- 当前 Android 清单里没有明确的明文 HTTP 放行配置
- Android 9+ 真机可能直接拦截 `http://...`
- 正式环境优先用域名 + HTTPS

### 方式 A：直接连 8080

只建议本地调试或确认已经允许明文 HTTP 时使用：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP:8080/api
```

### 方式 B：如果你已经用了 Nginx

推荐优先直接写成域名：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=https://nutriday.site/api
```

如果你还没配 HTTPS，只是临时走 HTTP 反代：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP/api
```

如果你指定设备，也可以这样：

```powershell
D:\flutter\bin\flutter.bat run -d 设备ID --dart-define=AI_HEALTH_API_BASE_URL=http://你的服务器IP:8080/api
```

---

## 十九、怎么判断“现在 App 能不能正常用了”

只有下面 4 条同时成立，才算真的可以用了：

1. `mysql -u aihealth -p ai_health -e "SELECT 1;"` 正常
2. `server.js` 启动日志显示 `AI Health API running on port 8080 (mysql)`
3. `curl http://127.0.0.1:8080/api/version/latest` 正常返回 JSON
4. Flutter App 启动时使用了 `--dart-define=AI_HEALTH_API_BASE_URL=...`

正式环境推荐把第 4 条固定成：

```powershell
D:\flutter\bin\flutter.bat run --dart-define=AI_HEALTH_API_BASE_URL=https://nutriday.site/api
```

如果只完成了“数据库建好”，但没启动后端，App 不能正常用。
如果后端已经连上 MySQL，但 App 还连着 `localhost`，App 也不能正常用。

---

## 二十、这次“完整功能”具体到什么程度

这轮不是把所有本地数据都搬到云端。
但下面这些核心链路，已经能走后端 + MySQL：

- 用户注册
- 用户登录 / 会话
- 成员管理
- 当前成员选择
- 补剂盒位绑定
- 设备绑定
- 健康数据上传
- 健康快照上传
- 计划读取 / 重算
- 报告分析
- 补剂分析
- 医生解释

仍然保留本地数据参与的部分，主要还是 Flutter 端的 Drift/SQLite 缓存与本地业务状态。

---

## 二十一、常见问题

### 1. 服务日志显示 `(local)` 不是 `(mysql)`

说明没有连上 MySQL。重点检查：

- `MYSQL_PASSWORD` 是否设置了
- `MYSQL_USER` 是否是 `aihealth`
- `MYSQL_DATABASE` 是否是 `ai_health`
- `init_ai_health.sql` 是否执行过
- `mysql -u aihealth -p ai_health -e "SELECT 1;"` 是否成功

### 2. App 打不开接口

重点检查：

- Flutter 是否仍在用 `localhost`
- 服务器安全组是否放行了 `8080`
- 若用了 Nginx，是否放行了 `80`
- 服务器本机执行 `curl http://127.0.0.1:8080/api/version/latest` 是否正常

### 3. 修改了后端代码但线上没变化

如果你用的是 `systemd`：

```bash
sudo systemctl restart ai-health-api
sudo journalctl -u ai-health-api -n 50 --no-pager
```

### 4. 重跑 SQL 会不会有问题

当前 [init_ai_health.sql](D:/Android-AI-Health/backend-example/sql/init_ai_health.sql) 使用了 `CREATE TABLE IF NOT EXISTS` 和部分 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`，通常可以重复执行。

---

## 二十二、最终上线核对清单

上线前只看这张表：

1. 整个 `backend-example` 已上传到服务器
2. MySQL 已安装并运行
3. `ai_health` 数据库已创建
4. `aihealth` 用户已创建并授权
5. `init_ai_health.sql` 已执行
6. `npm install` 已执行
7. `node server.js` 裸跑显示 `(mysql)`
8. `GET /api/version/latest` 正常
9. 注册、登录、成员、盒位绑定、设备绑定接口都能返回成功
10. `users`、`members`、`supplement_box_bindings`、`device_bindings` 表里能查到真实数据
11. `systemd` 服务已启用并显示 `active (running)`
12. Flutter 启动时已经传入 `AI_HEALTH_API_BASE_URL`

只要这 12 项都满足，你这套 App 的“后端 + MySQL”链路就算真正可用了。


