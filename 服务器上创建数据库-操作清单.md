# 在服务器上创建数据库 �?操作清单

> 适用：腾讯云 Ubuntu 服务器（�?`VM-0-17-ubuntu`）�?
> 在网页终端里一条一条执行，不要整段粘贴�?

---

## 一、安�?MySQL 服务

```bash
sudo apt update
sudo apt install -y mysql-server
```

```bash
sudo systemctl start mysql
sudo systemctl enable mysql
sudo systemctl status mysql
```

确认输出中有 `Active: active (running)`�?

---

## 二、安全初始化

```bash
sudo mysql_secure_installation
```

建议�?

- `Validate Password Plugin` �?`Y`
- 强度�?`1` �?`2`
- root 密码设为强密码并保存
- `Remove anonymous users?` �?`Y`
- `Disallow root login remotely?` �?`Y`
- `Remove test database?` �?`Y`
- `Reload privilege tables?` �?`Y`

---

## 三、创建数据库和用�?

把下面命令中�?`你的强密码` 换成你自己的密码�?

```bash
sudo mysql -e "
CREATE DATABASE IF NOT EXISTS ai_health CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'aihealth'@'localhost' IDENTIFIED BY '你的强密�?;
GRANT ALL PRIVILEGES ON ai_health.* TO 'aihealth'@'localhost';
FLUSH PRIVILEGES;
"
```

---

## 四、验证数据库是否可用

```bash
mysql -u aihealth -p ai_health -e "SELECT 'OK' AS status;"
```

输入密码后，如果看到 `status | OK`，说明数据库可用�?

也可以再看一次库列表�?

```bash
sudo mysql -e "SHOW DATABASES;"
```

应能看到 `ai_health`�?

---

## 五、执行项目建�?SQL

项目已经准备�?SQL：`backend-example/sql/init_ai_health.sql`

### 方式 A：先上传 SQL 文件再执�?

本机执行�?

```bash
scp backend-example/sql/init_ai_health.sql ubuntu@你的服务器IP:~/
```

服务器执行：

```bash
mysql -u aihealth -p ai_health < ~/init_ai_health.sql
```

### 方式 B：直接复�?SQL 内容执行

1. 打开 `backend-example/sql/init_ai_health.sql`
2. 复制全部内容
3. 服务器执行：

```bash
mysql -u aihealth -p ai_health
```

4. 粘贴 SQL
5. 执行完后输入�?

```sql
EXIT;
```

---

## 六、当前后端已使用哪些�?

当前 `backend-example/server.js` 会使用这些表�?

- `users`
- `auth_sessions`
- `health_data_log`
- `health_snapshots`
- `generated_plans`
- `analysis_records`
- `sync_events`
- `device_bindings`（预留）

也就是说�?

- Flutter App 仍然用本�?`Drift + SQLite`
- Node 后端在配置了 MySQL 后，会把登录、健康快照、计划和分析记录落到 MySQL
- 没配 `MYSQL_PASSWORD` 时，Node 后端会自动回退�?`backend-example/data/store.json`

---

## 七、启�?Node 后端（接 MySQL�?

在服务器上运行前先设置环境变量：

```bash
export MYSQL_HOST=127.0.0.1
export MYSQL_PORT=3306
export MYSQL_USER=aihealth
export MYSQL_PASSWORD='<YOUR_MYSQL_PASSWORD>'
export MYSQL_DATABASE=ai_health
```

然后启动�?

```bash
cd /opt/ai-health-api/backend-example
npm install
node server.js
```

也可以一行启动：

```bash
cd /opt/ai-health-api/backend-example
MYSQL_PASSWORD='<YOUR_MYSQL_PASSWORD>' node server.js
```

如果 MySQL 正常，启动日志会类似�?

```text
AI Health API running on port 8080 (mysql)
```

如果没配密码，会回退成：

```text
AI Health API running on port 8080 (local)
```

---

## 八、可直接验证的接�?

### 1. 版本接口

```bash
curl http://127.0.0.1:8080/api/version/latest
```

### 2. 注册接口

```bash
curl -X POST http://127.0.0.1:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"pass123456","displayName":"Test User"}'
```

### 3. 登录接口

```bash
curl -X POST http://127.0.0.1:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"pass123456"}'
```

返回里会�?`session.token`，后续接口都用它�?Bearer Token�?

---

## 九、后端当前已支持的接�?

- `GET /api/version`
- `GET /api/version/latest`
- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/auth/session`
- `POST /api/auth/logout`
- `POST /api/health-data`
- `POST /api/health/snapshots`
- `POST /api/report/analyze`
- `POST /api/supplement/analyze`
- `POST /api/plan/recompute`
- `GET /api/plan/latest`
- `POST /api/doctor/explain`

---

## 十、小�?

你在服务器上实际要做的是�?

1. 安装 MySQL
2. �?`mysql_secure_installation`
3. 创建 `ai_health` 数据库和 `aihealth` 用户
4. 执行 `backend-example/sql/init_ai_health.sql`
5. 设置 `MYSQL_PASSWORD` 等环境变�?
6. �?`backend-example` 目录运行 `node server.js`

做完后，这套后端就不是“只会收内存数据�?demo 接口”了，而是可以真正把账号、健康快照和计划结果落到 MySQL�?

---

## 十一、下一步：部署 Node 服务

数据库准备完后，下一步看这份部署清单�?

- [服务器上部署后端-操作清单.md](/D:/Android-AI-Health/服务器上部署后端-操作清单.md)

里面已经补了�?

- 环境变量文件
- `node server.js` 裸跑验证
- `systemd` 守护
- `pm2` 守护
- `nginx` 反向代理
- Flutter 真机连服务器�?`--dart-define` 启动方式



